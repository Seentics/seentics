FROM node:20-alpine

WORKDIR /app

# postinstall runs `bundle-trackers` — needs tracker sources before `npm install`
COPY package.json package-lock.json* ./
COPY public/trackers ./public/trackers/
RUN npm install --legacy-peer-deps

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
