/**
 * Shared layout for /websites/[websiteId]/* pages so support, billing, settings,
 * and dashboards align to the same max width and padding.
 */
export const websiteWorkspaceShellClass =
  'w-full max-w-[1440px] mx-auto px-4 py-6 md:px-6 md:py-8 lg:px-8';
