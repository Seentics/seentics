/**
 * @deprecated Import from `@/features/agency/*` instead.
 *
 * Transitional re-export while callers move to the feature module. Nothing new here.
 */
export * from '../features/agency/types';
export * from '../features/agency/api';
