/**
 * @deprecated Import from `@/features/replays/*` instead.
 *
 * Transitional re-export while callers move to the feature module. Nothing new here.
 */
export * from '../features/replays/types';
export * from '../features/replays/api';
