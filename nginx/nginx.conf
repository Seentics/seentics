events {
    worker_connections 1024;
}

http {
    server_tokens off;
    charset utf-8;

    # Backend API (api.seentics.com)
    server {
        listen 80;
        server_name api.seentics.com localhost;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location /trackers/ {
            alias /usr/share/nginx/html/trackers/;
            add_header Access-Control-Allow-Origin "*";
            add_header Cache-Control "public, max-age=3600";
            try_files $uri =404;
        }

        location / {
            proxy_pass http://analytics-backend:3002;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # SSL block commented out for local development
    # server {
    #     listen 443 ssl;
    #     server_name api.seentics.com;
    #
    #     ssl_certificate /etc/letsencrypt/live/api.seentics.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/api.seentics.com/privkey.pem;
    #     include /etc/nginx/ssl/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/nginx/ssl/ssl-dhparams.pem;
    #
    #     location /trackers/ {
    #         alias /usr/share/nginx/html/trackers/;
    #         add_header Access-Control-Allow-Origin "*";
    #         add_header Cache-Control "public, max-age=3600";
    #         try_files $uri =404;
    #     }
    #
    #     location / {
    #         proxy_pass http://analytics-backend:3002;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
