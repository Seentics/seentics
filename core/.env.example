# Seentics Analytics Backend - Environment Configuration

# Server Configuration
ENVIRONMENT=development
PORT=3002

# Database Configuration (PostgreSQL)
DATABASE_URL=postgres://seentics:seentics_postgres_pass@localhost:5432/seentics_analytics?sslmode=disable

# Redis Configuration
REDIS_URL=redis://:seentics_redis_pass@localhost:6379

# NATS Configuration (Event Streaming)
NATS_URL=nats://localhost:4222
NATS_SUBJECT_EVENTS=analytics.events

# Security
JWT_SECRET=your-super-secret-jwt-key
GLOBAL_API_KEY=your-global-api-key # Used for inter-service auth if CLOUD_ENABLED=true
CLOUD_ENABLED=false

# Backend enterprise mode (set by enterprise gateway, leave false for OSS)
IS_ENTERPRISE=false

# Logging
LOG_LEVEL=info
